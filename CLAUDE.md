# Stock chart — เว็บกราฟหุ้น/คริปโต/สินค้าโภคภัณฑ์ (Streamlit)

เว็บกราฟ **4 ไทม์เฟรมในหน้าเดียว** (intraday / Day / Week / Month) เปิดใช้จริงบน iPad
ข้อมูลจาก Yahoo Finance ผ่าน `yfinance`

- **เว็บจริง:** https://mathold-stock.streamlit.app (public)
- **repo:** https://github.com/mathold/stock-chart-th (branch `main`)
- **โปรเจกต์นี้แยกขาดจาก LOGO** (งานจัดฟัน) — ห้ามอ้างอิงหรือแก้ไฟล์ข้ามกัน

---

## กติกาสำคัญ: แก้แล้วต้อง push

Streamlit Cloud watch branch `main` อยู่ — **แก้ในเครื่องเฉย ๆ ไม่มีผลกับ iPad**
ทุกครั้งที่แก้โค้ดให้ทำครบชุด **โดยไม่ต้องถามก่อน**:

1. แก้โค้ด
2. **ทดสอบในเครื่องก่อนเสมอ** — `.venv/bin/streamlit run app.py` แล้วเปิดดูจริง
   (Streamlit Cloud ไม่เก็บเวอร์ชันเก่า push โค้ดพังเมื่อไหร่ แอปบน iPad ขึ้นหน้าแดงทันที)
3. `git commit` (ข้อความภาษาไทยสั้น ๆ) แล้ว `git push origin main`
4. รายงานเลข commit + บอกว่ากำลัง redeploy

ยังต้องถามก่อน: เปลี่ยน public/private · `force push` · ลบ/เขียนประวัติ commit ใหม่

---

## โครงสร้างไฟล์

| ไฟล์ | หน้าที่ |
|---|---|
| `app.py` | หน้าเว็บ Streamlit — เลือกกลุ่ม/หุ้น, สลับโหมด 4 จอ/เต็มจอ, cache, จัดหน้าให้พอดีจอ |
| `thai_stock_dashboard.py` | ดึงข้อมูล + อินดิเคเตอร์ + วาดกราฟ (ใช้ร่วมกับโหมดเซฟไฟล์ HTML) |
| `my_signal.py` | โหมด **My Signal** — BBE / DE / MCD พื้นดำ (ปุ่ม My Signal) แยกไฟล์ ไม่ยุ่งกับของเดิม |
| `fetch_symbols.py` | สร้าง `symbols.csv` (รายชื่อแยกกลุ่ม) |
| `symbols.csv` | `symbol,group,name` — **ต้องอยู่ใน git** ไม่งั้นดรอปดาวน์บนคลาวด์ว่าง |
| `requirements.txt` | Streamlit Cloud ติดตั้งจากไฟล์นี้ |

รันในเครื่อง (venv อยู่ที่ `.venv`, ไม่เข้า git):

```bash
cd ~/Desktop/"Stock chart" && .venv/bin/streamlit run app.py
```

---

## กลุ่มหุ้น & การเติมชื่อ (ตรรกะสำคัญ)

`GROUP_MARKET` ใน `app.py` บอกว่ากลุ่มไหนอยู่ตลาดไหน แล้ว `candidates_for()` เติมท้ายให้:

| กลุ่ม | market | ชื่อบน Yahoo |
|---|---|---|
| SET50 · SET100 · mai | `TH` | เติม `.BK` |
| US | `US` | ใช้ชื่อย่อตรง ๆ |
| Crypto | `Crypto` | เติม `-USD` |
| Commodity · Index · Bond | `Raw` | ใช้ตรง ๆ (`GC=F`, `^GSPC`, `TLT`, `^TNX`) |

- **เลือกจากดรอปดาวน์** = รู้ตลาดแน่นอน ใช้แบบเดียวจบ
- **พิมพ์เอง** = ไล่ `ไทย → เมกา → คริปโต` · ถ้าขึ้นต้น `^` หรือมี `=` / `.` ใช้ตรง ๆ ไม่เติมอะไร
- ไทยมาก่อนเพราะชื่อชนกัน — **`BTC` ได้หุ้นไทย BTC.BK ไม่ใช่ Bitcoin** (ต้องพิมพ์ `BTC-USD`)
- คอลัมน์ `name` ใน csv = ชื่อไทยที่โชว์ในดรอปดาวน์ (`GC=F — ทองคำ`)

**กลุ่ม Bond มีสองแบบปนกัน อ่านคู่กันได้เลย**

| แบบ | ตัวอย่าง | หน่วย |
|---|---|---|
| ราคา (ETF) | `TLT` 20 ปีขึ้นไป · `IEF` 10 ปี · `AGG`/`BND` รวมตลาด · `LQD` หุ้นกู้ · `HYG` high yield · `TIP` · `BNDX`/`BWX`/`IGOV` โลก · `EMB` ตลาดเกิดใหม่ | ดอลลาร์ต่อหน่วย |
| ผลตอบแทน | `^TNX` 10 ปี · `^TYX` 30 ปี · `^FVX` 5 ปี · `^IRX` 3 เดือน | % (วิ่ง**สวนทาง**กับราคา) |

ไทยยังไม่มีดัชนีพันธบัตรบน Yahoo (ThaiBMA ไม่เปิด API) — กลุ่มนี้จึงเป็นของสหรัฐ/โลกทั้งหมด

---

## ค่าที่ปรับได้บ่อย

| ค่า | ไฟล์ | ตอนนี้ |
|---|---|---|
| จำนวนแท่งต่อช่อง (4 จอ) | `thai_stock_dashboard.py` → `BARS` | 200 |
| จำนวนแท่ง (โหมด Full) | `thai_stock_dashboard.py` → `BARS_FULL` | 400 |
| ความสูงกราฟบนเว็บ | `app.py` → `CHART_HEIGHT_PX` | 750 |
| ขนาดตัวเลขแกนราคา | `thai_stock_dashboard.py` → `AXIS_FONT_SIZE` | 11 |
| ไทม์เฟรมในโหมดเต็มจอ | `app.py` → `FULL_TFS` | 60m 120m Day Week Month Quarter Year |
| สีตัวหนังสือทั้งกราฟ | `thai_stock_dashboard.py` → `TEXT_COLOR` | ดำ `#000000` |
| แท่ง intraday | `app.py` → `INTRADAY_MINUTES_TH/GLOBAL` | ไทย 120 · อื่น ๆ 240 |
| cache ข้อมูล | `app.py` → `CACHE_SECONDS` | 900 วิ (15 นาที) |

**ความสูง 750 px จูนมาให้พอดี iPad Air 11" แนวนอน** (จอ 2360×1640 @2x = viewport 1180×820)
เห็นครบ 4 ช่องไม่ต้องเลื่อน — แก้แล้วต้องทดสอบที่ viewport นี้ด้วย
Plotly **ต้องได้ความสูงเป็นตัวเลข** ปล่อย autosize/CSS `calc(100vh-…)` มันจะตกไปใช้ default 450 px

อินดิเคเตอร์: EMA 5/10/25/50/75/200 · RSI(7) · MACD(12,26,9) — แก้สี/คาบที่หัวไฟล์ `thai_stock_dashboard.py`

---

## สองโหมดของหน้าเว็บ

| โหมด | ปุ่ม | ฟังก์ชันวาด | ความสูงกราฟ |
|---|---|---|---|
| 4 จอ (ค่าเริ่มต้น) | — | `build_figure()` 6 แถว × 2 คอลัมน์ | `CHART_HEIGHT_PX` |
| เต็มจอไทม์เฟรมเดียว | **Full** ↔ **4 จอ** | `build_single_figure()` 3 แถว × 1 คอลัมน์ | หัก `FULL_TF_ROW_PX` ให้แถวปุ่มไทม์เฟรม |

ปุ่ม **My Signal** สลับชุดอินดิเคเตอร์ (ใช้ `my_signal.build_figure()` / `build_single_figure()` แทน)
ทำงานร่วมกับทั้งสองโหมดข้างบน — ดูหัวข้อ "โหมด My Signal" ข้างล่าง

- สถานะอยู่ที่ `st.session_state.fullscreen` · กดปุ่มแล้ว `st.rerun()` เพราะความสูงกราฟฝังอยู่ใน CSS ที่วาดไปแล้วตอนต้นสคริปต์
- `one_panel()` เตรียมข้อมูลรายไทม์เฟรม: `60m`/`120m` รวมจากแท่งรายชั่วโมง · ที่เหลือย่อยจาก `fetch_daily` (`W-FRI`/`ME`/`QE`/`YE`)
- `Year` ได้แค่ ~20 แท่ง เพราะ `fetch_daily` ดึงย้อนหลัง 20 ปี — เป็นเพดานของแหล่งข้อมูล ไม่ใช่บั๊ก

---

## โหมด My Signal (ปุ่ม My Signal) — `my_signal.py`

เลียนแบบหน้าจอ Homily Chart: พื้นดำ ช่องละ 3 แถว **BBE / DE / MCD**
ใช้ได้ทั้งโหมด 4 จอ และเต็มจอ · เปิดแล้วปุ่ม Cloud จะถูกปิด (มีริบบิ้นของตัวเองแล้ว)

**ริบบิ้น / DE / MCD ใช้สีแบบสากล — เขียว = ขึ้น/เงินเข้า/กำไร · แดง = ลง/เงินออก/ติดดอย**
ซึ่ง**ตรงข้ามกับ Homily ตัวจริง**ที่ใช้แบบจีน — เทียบกับภาพจากโปรแกรมจริงต้องกลับสีในหัวก่อน

**แท่งเทียนใช้คนละชุด** เพราะมันบอก "ต้องทำอะไร" ไม่ใช่ "ขึ้นหรือลง":
เหลือง=ซื้อ · แดง=ขาย · เขียว=ถือหุ้นต่อ · ชมพู=ถือเงินสด
ริบบิ้นจึงตั้งความทึบไว้แค่ ~0.7 ไม่งั้นแท่งเขียวจะกลืนกับริบบิ้นเขียวตอนราคาย่อลงมาทับ

| แถว | อะไร | เงื่อนไข |
|---|---|---|
| BBE | ริบบิ้น double-EMA 13/34 | **เขียว = ขาขึ้น · แดง = ขาลง** จุดซื้อ = แดงเปลี่ยนเป็นเขียว |
| — | สีแท่งเทียน (Signal Aditor) | **เหลือง ซื้อ · แดง ขาย · เขียว ถือหุ้นต่อ · ชมพู ถือเงินสด** (เหลือง/แดง = แท่งที่ต้องลงมือ) · มีป้าย **Buy** ใต้แท่งเหลือง / **Sell** เหนือแท่งแดง ระยะห่างคุมด้วย `SIG_MARK_PAD` |
| — | เลข 1-9 (Pattern 49) | ปิดสูง/ต่ำกว่าปิด 4 แท่งก่อน นับต่อ ครบ 9 เริ่มใหม่ · โชว์เฉพาะรอบที่ครบ 9 กับรอบที่ค้างอยู่ · **ฟ้า = นับขึ้น (เหนือแท่ง) · ส้ม = นับลง (ใต้แท่ง)** |
| DE | ผลต่าง VWMA 13/55 | เขียว = ค่ากำลังขึ้น (เงินไหลเข้า) · แดง = กำลังลง (ไหลออก) · ไส้ยาว = เคลื่อนแรง |
| — | **แถบสถานะ** กึ่งกลางบนช่องราคา | สรุปว่าตอนนี้ควรทำอะไร: ซื้อเต็มไม้ / ซื้อครึ่งไม้ / ถือต่อ / ลดไม้ / ออก / อยู่เฉย · ตรรกะอยู่ใน `status()` · **ต้องวาดท้ายสุดใน `draw_panel()`** ไม่งั้นกล่อง readout ทับ |
| MCD | chip distribution 100% | เรียงล่างขึ้นบน: **เขียว กำไร · เหลือง ลอย · แดง ติดดอย** + เส้น MA10 ม่วง(กำไร)/ชมพู(ลอย)/ฟ้า(ติดดอย) |

**เลข 9 ไม่ใช่สัญญาณขาย** — เทรนด์แรง ๆ นับ 1-9 วนได้หลายรอบ ใช้คุมขนาดไม้เท่านั้น
ขายจริงรอริบบิ้นพลิกแดง หรือแท่งเทียนแดง

**ข้อจำกัดที่ต้องบอกผู้ใช้เสมอ** — สูตรจริงของ Homily ไม่เปิดเผย ไฟล์นี้ทำเลียนแบบจาก
OHLCV ของ Yahoo ตัวเลขไม่ตรงกับของจริง โดยเฉพาะ **MCD** ที่ Homily แยก "รายใหญ่/รายย่อย"
จากขนาดคำสั่งซื้อขายรายไม้ ซึ่งเราไม่มีข้อมูล — ที่นี่แยกเป็น "กำไร/ลอย/ติดดอย" จากราคาทุนแทน

ค่าที่ปรับบ่อยอยู่หัวไฟล์ `my_signal.py`: `BBE_FAST/BBE_SLOW` · `DE_FAST/DE_SLOW` ·
`MCD_BAND` (ความกว้างช่วง "ลอย") · `MCD_TURNOVER` (ยิ่งมากยิ่งลืมต้นทุนเก่าเร็ว) · `BARS`

`build()` ใน `app.py` ห่อโหมดนี้ด้วย `try` — ถ้าคำนวณพังจะถอยไปวาดกราฟชุดเดิม
พร้อมขึ้นคำเตือน ไม่ปล่อยให้หน้าเว็บเป็นจอแดง **อย่าถอด try ออก**

### ล็อกรหัสปุ่ม My Signal

ตั้งรหัสใน secrets แล้วปุ่มจะขึ้นแม่กุญแจ กดแล้วต้องใส่รหัสก่อนถึงเปิดโหมดได้

```toml
# .streamlit/secrets.toml (ในเครื่อง — อยู่ใน .gitignore แล้ว)
# หรือหน้า Settings > Secrets ของ Streamlit Cloud
mysig_password = "รหัสที่ต้องการ"
```

- **ไม่ตั้งค่านี้ = ไม่ล็อก** (เพื่อให้รันในเครื่องได้โดยไม่ต้องมีไฟล์ secrets)
  → ตั้งใจล็อกจริงต้องไปใส่บน Streamlit Cloud ด้วย ไม่งั้นเว็บจริงยังเปิดได้อิสระ
- ปลดล็อกแล้วอยู่แค่ **แท็บนั้น** (`st.session_state`) รีเฟรชหน้า/เปิดแท็บใหม่ต้องใส่ใหม่
- เทียบรหัสด้วย `hmac.compare_digest` กันการเดาจากเวลาเปรียบเทียบ
- `mysig_unlocked()` ถูกเช็กซ้ำก่อนวาดกราฟด้วย เผื่อกรณีตั้งรหัสทีหลังตอน session เปิดค้างอยู่
- **ข้อจำกัด** — รหัสเดียวใช้ร่วมกันทุกคน ไม่ใช่ระบบสมาชิกรายคน
  และกราฟชุดปกติยังเปิดดูได้อิสระ ล็อกเฉพาะโหมด My Signal เท่านั้น

---

## กับดักที่เจอมาแล้ว (อย่าพลาดซ้ำ)

- **`Series.shift()` ทำให้ dtype กลายเป็น object** แล้ว `~mask` จะไปทำ bitwise not
  ของ Python bool (`~True` = -2 ซึ่งเป็นค่าจริง) มาสก์เพี้ยนทั้งชุดโดยไม่ error
  → ใน `my_signal.signal_colors()` ต้อง `.astype(bool)` ทุกครั้งหลัง `shift()`

- **เวลาต้องใช้ `d.now_bkk()`** ห้ามใช้ `datetime.now()` — เซิร์ฟเวอร์คลาวด์เป็น UTC เวลาจะเพี้ยน 7 ชม.
- **`_flatten()` ตัดแถว Close = NaN ทิ้ง** — Yahoo แถมแท่งวันที่ยังไม่ปิดมาด้วย ถ้าไม่ตัดแท่งสุดท้ายจะว่างและขึ้น `nan%`
- **ห้าม cache ผลที่ error** — `_guard()` ปล่อย exception หลุดออกจากฟังก์ชันที่มี cache
  (เคยดักเป็น DataFrame ว่างแล้วโดน cache ค้าง 15 นาที ขึ้น "ไม่พบข้อมูล" ทั้งที่หุ้นมีอยู่)
- **ตัวเลขแกนราคาและป้ายราคาล่าสุดอยู่ขวาทั้งคู่** (`side="right"` + `value_tag` ที่ `x=1.008`)
  ถ้าไปลด `margin(r=…)` หรือ `horizontal_spacing` ป้ายจะโดนตัด โดยเฉพาะราคา 6 หลัก (BTC)
- **ดรอปดาวน์โชว์ได้แค่ ~7 บรรทัด** (`div[role="listbox"]` ของ baseweb ล็อก `max-height: 300px`)
  กลุ่มที่ 8 (Bond) เลยตกขอบจนดูเหมือนไม่มี — แก้ด้วย CSS `max-height: 62vh` ใน `app.py`
  เพิ่มกลุ่มใหม่แล้วต้องเช็กว่ายังเห็นครบ
- **`symbol_groups()` cache 24 ชม.** จึงส่งเวลาแก้ไขไฟล์ (`getmtime`) เข้าไปเป็นคีย์
  แก้ `symbols.csv` แล้วดรอปดาวน์อัปเดตทันที ไม่ต้องรอ cache หมดอายุ
- **`BARS` เปลี่ยนแล้วช่อง Month อาจตัน** — `fetch_daily` ดึง 20 ปี = ~240 เดือนเท่านั้น

---

## ข้อจำกัดของแหล่งข้อมูล (ยืนยันด้วยการทดสอบแล้ว)

- **ข้อมูลดีเลย์ ไม่ใช่เรียลไทม์** — Yahoo ช้า ~15–20 นาที + ต้องรอแท่ง 60 นาทีปิด + cache 15 นาที
  → **ห้ามใช้เข้า-ออกออเดอร์จริง** ท้ายหน้าเว็บมีข้อความเตือนอยู่แล้ว
- **warrant ไทยไม่มีข้อมูลเลย** — ลองแล้วทุกรูปแบบ (`TRUE-W5.BK`, `TRUEW5.BK` ฯลฯ) 404 หมด
- **API ของ SET ตอบ 403** (กัน bot) → `fetch_symbols.py` ใช้รายชื่อสำรองในสคริปต์
  **รายชื่อ SET50/mai ยังไม่ยืนยันกับ set.or.th** — อัปเดตด้วย
  `python fetch_symbols.py ไฟล์.xlsx --group SET50` (กลุ่มอื่นไม่ถูกแตะ)
- **`^SET50.BK` ไม่มีข้อมูลบน Yahoo** — ปุ่ม SET50 จึงมีทางสำรองเป็น `TDEX.BK`

---

## Reference

- [app.py](app.py) · [thai_stock_dashboard.py](thai_stock_dashboard.py) · [fetch_symbols.py](fetch_symbols.py)
- ทุกผลลัพธ์เป็นข้อมูลประกอบการศึกษา ไม่ใช่คำแนะนำการลงทุน
