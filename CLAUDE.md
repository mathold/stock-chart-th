# Stock chart — เว็บกราฟหุ้น/คริปโต/สินค้าโภคภัณฑ์ (Streamlit)

เว็บกราฟ **4 ไทม์เฟรมในหน้าเดียว** (intraday / Day / Week / Month) เปิดใช้จริงบน iPad
ข้อมูลจาก Yahoo Finance ผ่าน `yfinance`

- **เว็บจริง:** https://mathold-stock.streamlit.app (public)
- **repo:** https://github.com/mathold/stock-chart-th (branch `main`)
- **โปรเจกต์นี้แยกขาดจาก LOGO** (งานจัดฟัน) — ห้ามอ้างอิงหรือแก้ไฟล์ข้ามกัน

---

## กติกาสำคัญ: แก้แล้วต้อง push

Streamlit Cloud watch branch `main` อยู่ — **แก้ในเครื่องเฉย ๆ ไม่มีผลกับ iPad**
ทุกครั้งที่แก้โค้ดให้ทำครบชุด **โดยไม่ต้องถามก่อน**:

1. แก้โค้ด
2. **ทดสอบในเครื่องก่อนเสมอ** — `.venv/bin/streamlit run app.py` แล้วเปิดดูจริง
   (Streamlit Cloud ไม่เก็บเวอร์ชันเก่า push โค้ดพังเมื่อไหร่ แอปบน iPad ขึ้นหน้าแดงทันที)
3. `git commit` (ข้อความภาษาไทยสั้น ๆ) แล้ว `git push origin main`
4. รายงานเลข commit + บอกว่ากำลัง redeploy

ยังต้องถามก่อน: เปลี่ยน public/private · `force push` · ลบ/เขียนประวัติ commit ใหม่

---

## โครงสร้างไฟล์

| ไฟล์ | หน้าที่ |
|---|---|
| `app.py` | หน้าเว็บ Streamlit — เลือกกลุ่ม/หุ้น, cache, จัดหน้าให้พอดีจอ |
| `thai_stock_dashboard.py` | ดึงข้อมูล + อินดิเคเตอร์ + วาดกราฟ (ใช้ร่วมกับโหมดเซฟไฟล์ HTML) |
| `fetch_symbols.py` | สร้าง `symbols.csv` (รายชื่อแยกกลุ่ม) |
| `symbols.csv` | `symbol,group,name` — **ต้องอยู่ใน git** ไม่งั้นดรอปดาวน์บนคลาวด์ว่าง |
| `requirements.txt` | Streamlit Cloud ติดตั้งจากไฟล์นี้ |

รันในเครื่อง (venv อยู่ที่ `.venv`, ไม่เข้า git):

```bash
cd ~/Desktop/"Stock chart" && .venv/bin/streamlit run app.py
```

---

## กลุ่มหุ้น & การเติมชื่อ (ตรรกะสำคัญ)

`GROUP_MARKET` ใน `app.py` บอกว่ากลุ่มไหนอยู่ตลาดไหน แล้ว `candidates_for()` เติมท้ายให้:

| กลุ่ม | market | ชื่อบน Yahoo |
|---|---|---|
| SET50 · SET100 · mai | `TH` | เติม `.BK` |
| US | `US` | ใช้ชื่อย่อตรง ๆ |
| Crypto | `Crypto` | เติม `-USD` |
| Commodity · Index · Bond | `Raw` | ใช้ตรง ๆ (`GC=F`, `^GSPC`, `TLT`, `^TNX`) |

- **เลือกจากดรอปดาวน์** = รู้ตลาดแน่นอน ใช้แบบเดียวจบ
- **พิมพ์เอง** = ไล่ `ไทย → เมกา → คริปโต` · ถ้าขึ้นต้น `^` หรือมี `=` / `.` ใช้ตรง ๆ ไม่เติมอะไร
- ไทยมาก่อนเพราะชื่อชนกัน — **`BTC` ได้หุ้นไทย BTC.BK ไม่ใช่ Bitcoin** (ต้องพิมพ์ `BTC-USD`)
- คอลัมน์ `name` ใน csv = ชื่อไทยที่โชว์ในดรอปดาวน์ (`GC=F — ทองคำ`)

**กลุ่ม Bond มีสองแบบปนกัน อ่านคู่กันได้เลย**

| แบบ | ตัวอย่าง | หน่วย |
|---|---|---|
| ราคา (ETF) | `TLT` 20 ปีขึ้นไป · `IEF` 10 ปี · `AGG`/`BND` รวมตลาด · `LQD` หุ้นกู้ · `HYG` high yield · `TIP` · `BNDX`/`BWX`/`IGOV` โลก · `EMB` ตลาดเกิดใหม่ | ดอลลาร์ต่อหน่วย |
| ผลตอบแทน | `^TNX` 10 ปี · `^TYX` 30 ปี · `^FVX` 5 ปี · `^IRX` 3 เดือน | % (วิ่ง**สวนทาง**กับราคา) |

ไทยยังไม่มีดัชนีพันธบัตรบน Yahoo (ThaiBMA ไม่เปิด API) — กลุ่มนี้จึงเป็นของสหรัฐ/โลกทั้งหมด

---

## ค่าที่ปรับได้บ่อย

| ค่า | ไฟล์ | ตอนนี้ |
|---|---|---|
| จำนวนแท่งต่อช่อง | `thai_stock_dashboard.py` → `BARS` | 200 |
| ความสูงกราฟบนเว็บ | `app.py` → `CHART_HEIGHT_PX` | 750 |
| ขนาดตัวเลขแกนราคา | `thai_stock_dashboard.py` → `AXIS_FONT_SIZE` | 11 |
| สีตัวหนังสือทั้งกราฟ | `thai_stock_dashboard.py` → `TEXT_COLOR` | ดำ `#000000` |
| แท่ง intraday | `app.py` → `INTRADAY_MINUTES_TH/GLOBAL` | ไทย 120 · อื่น ๆ 240 |
| cache ข้อมูล | `app.py` → `CACHE_SECONDS` | 900 วิ (15 นาที) |

**ความสูง 750 px จูนมาให้พอดี iPad Air 11" แนวนอน** (จอ 2360×1640 @2x = viewport 1180×820)
เห็นครบ 4 ช่องไม่ต้องเลื่อน — แก้แล้วต้องทดสอบที่ viewport นี้ด้วย
Plotly **ต้องได้ความสูงเป็นตัวเลข** ปล่อย autosize/CSS `calc(100vh-…)` มันจะตกไปใช้ default 450 px

อินดิเคเตอร์: EMA 5/10/25/50/75/200 · RSI(7) · MACD(12,26,9) — แก้สี/คาบที่หัวไฟล์ `thai_stock_dashboard.py`

---

## กับดักที่เจอมาแล้ว (อย่าพลาดซ้ำ)

- **เวลาต้องใช้ `d.now_bkk()`** ห้ามใช้ `datetime.now()` — เซิร์ฟเวอร์คลาวด์เป็น UTC เวลาจะเพี้ยน 7 ชม.
- **`_flatten()` ตัดแถว Close = NaN ทิ้ง** — Yahoo แถมแท่งวันที่ยังไม่ปิดมาด้วย ถ้าไม่ตัดแท่งสุดท้ายจะว่างและขึ้น `nan%`
- **ห้าม cache ผลที่ error** — `_guard()` ปล่อย exception หลุดออกจากฟังก์ชันที่มี cache
  (เคยดักเป็น DataFrame ว่างแล้วโดน cache ค้าง 15 นาที ขึ้น "ไม่พบข้อมูล" ทั้งที่หุ้นมีอยู่)
- **ตัวเลขแกนราคาอยู่ซ้าย · ป้ายราคาล่าสุดอยู่ขวา** (`value_tag` ใช้ `x=1.008` นอกกรอบ)
  ถ้าไปลด `margin(r=…)` หรือ `horizontal_spacing` ป้ายจะโดนตัด โดยเฉพาะราคา 6 หลัก (BTC)
- **`BARS` เปลี่ยนแล้วช่อง Month อาจตัน** — `fetch_daily` ดึง 20 ปี = ~240 เดือนเท่านั้น

---

## ข้อจำกัดของแหล่งข้อมูล (ยืนยันด้วยการทดสอบแล้ว)

- **ข้อมูลดีเลย์ ไม่ใช่เรียลไทม์** — Yahoo ช้า ~15–20 นาที + ต้องรอแท่ง 60 นาทีปิด + cache 15 นาที
  → **ห้ามใช้เข้า-ออกออเดอร์จริง** ท้ายหน้าเว็บมีข้อความเตือนอยู่แล้ว
- **warrant ไทยไม่มีข้อมูลเลย** — ลองแล้วทุกรูปแบบ (`TRUE-W5.BK`, `TRUEW5.BK` ฯลฯ) 404 หมด
- **API ของ SET ตอบ 403** (กัน bot) → `fetch_symbols.py` ใช้รายชื่อสำรองในสคริปต์
  **รายชื่อ SET50/mai ยังไม่ยืนยันกับ set.or.th** — อัปเดตด้วย
  `python fetch_symbols.py ไฟล์.xlsx --group SET50` (กลุ่มอื่นไม่ถูกแตะ)
- **`^SET50.BK` ไม่มีข้อมูลบน Yahoo** — ปุ่ม SET50 จึงมีทางสำรองเป็น `TDEX.BK`

---

## Reference

- [app.py](app.py) · [thai_stock_dashboard.py](thai_stock_dashboard.py) · [fetch_symbols.py](fetch_symbols.py)
- ทุกผลลัพธ์เป็นข้อมูลประกอบการศึกษา ไม่ใช่คำแนะนำการลงทุน
