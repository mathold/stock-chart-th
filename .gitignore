# virtualenv
.venv/
venv/

# python
__pycache__/
*.py[cod]

# streamlit
.streamlit/secrets.toml

# ผลลัพธ์กราฟ HTML ที่ thai_stock_dashboard.py เซฟไว้
StockCharts/

# macOS
.DS_Store

# ภาพที่เอาไว้ให้ Claude อ่าน (ไม่ต้องขึ้น git)
images/

# สคริปต์ push ในเครื่อง (ดับเบิลคลิกจาก Finder) — ไม่ต้องขึ้น git
push.command

# ที่กองไฟล์รอลบ — เครื่องเสมือนของ Claude ลบไฟล์เองไม่ได้ ทำได้แค่ย้ายมากอง
_to_delete/
