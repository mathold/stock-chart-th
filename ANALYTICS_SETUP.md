# ตั้งตัวนับผู้ใช้ลง Google Sheet — 5 ขั้นตอน

ทำครั้งเดียวประมาณ 5 นาที หลังจากนี้ทุกครั้งที่มีคนเปิดเว็บหรือกดปุ่ม My Signal
จะมีบรรทัดใหม่วิ่งเข้าชีตเอง

---

## 1. สร้าง Google Sheet เปล่า

เข้า [sheets.new](https://sheets.new) → ตั้งชื่อ เช่น **mathold-stock analytics**
(ไม่ต้องสร้างแท็บหรือหัวตารางเอง สคริปต์สร้างให้)

## 2. วางโค้ด Apps Script

ในชีตนั้น → เมนู **Extensions › Apps Script**
→ ลบโค้ด `function myFunction() {}` ที่มีอยู่ให้หมด
→ เปิดไฟล์ **`analytics_appscript.gs`** ในโฟลเดอร์โปรเจกต์ คัดลอกทั้งไฟล์มาวาง

แก้บรรทัดบน ๆ ให้เป็นรหัสลับที่พี่ตั้งเอง (อะไรก็ได้ ห้ามบอกใคร):

```js
var SECRET_KEY = 'matholdstock2026xyz';
```

กด **Save** (รูปแผ่นดิสก์)

## 3. Deploy เป็น Web app

กด **Deploy › New deployment** → ไอคอนเฟือง เลือก **Web app** → ตั้งค่า:

| ช่อง | เลือก |
|---|---|
| Description | อะไรก็ได้ เช่น v1 |
| Execute as | **Me (อีเมลพี่)** |
| Who has access | **Anyone** |

กด **Deploy** → Google จะขอสิทธิ์ → **Authorize access** → เลือกบัญชี →
ถ้าขึ้นหน้าจอ "Google hasn't verified this app" ให้กด **Advanced** →
**Go to ... (unsafe)** → **Allow**
(เป็นสคริปต์ของพี่เอง ที่ขึ้นเตือนเพราะยังไม่ได้ส่ง Google ตรวจ)

จะได้ URL หน้าตาแบบนี้ — **คัดลอกเก็บไว้**

```
https://script.google.com/macros/s/AKfycb....../exec
```

> ⚠️ "Who has access: Anyone" จำเป็นเพราะเซิร์ฟเวอร์ Streamlit ต้องยิงเข้ามาโดยไม่ล็อกอิน
> ความปลอดภัยอยู่ที่ `SECRET_KEY` — คนที่ไม่รู้รหัสยิงเข้ามาก็ไม่ถูกบันทึก
> และตัวชีตยังเป็นส่วนตัวของพี่คนเดียวอยู่

## 4. ใส่ค่าใน Streamlit Cloud

เข้า [share.streamlit.io](https://share.streamlit.io) → จุดสามจุด (⋮) ข้างแอป
→ **Settings › Secrets** → เพิ่ม 2 บรรทัดนี้ต่อจากของเดิม (อย่าลบ `mysig_password`):

```toml
analytics_url = "https://script.google.com/macros/s/AKfycb....../exec"
analytics_key = "matholdstock2026xyz"
```

กด **Save** → แอปจะรีสตาร์ทเองประมาณ 30 วินาที

> ถ้าอยากทดสอบในเครื่องด้วย ใส่ 2 บรรทัดเดียวกันใน `.streamlit/secrets.toml`
> (ไฟล์นั้นอยู่ใน `.gitignore` แล้ว ไม่หลุดขึ้น GitHub)

## 5. ทดสอบ

เปิดเว็บ → กดปุ่ม **My Signal** → กลับไปดูชีต
จะเห็นแท็บ **log** มีบรรทัดใหม่ขึ้นมาภายในไม่กี่วินาที

---

## ดูสถิติยังไง

**ในชีต** — แท็บ **สรุป** บอก:

- ผู้ใช้ทั้งหมด (นับแท็บที่ไม่ซ้ำ)
- จำนวนครั้งที่เปิดเว็บ
- กดปุ่ม My Signal กี่ครั้ง / เข้าโหมดสำเร็จกี่ครั้ง / เจอกำแพงรหัสกี่ครั้ง
- ตารางรายวัน

ตัวเลขอัปเดตทุกครั้งที่เปิดชีต หรือกดเมนู **📊 สถิติ › อัปเดตสรุป**
(ครั้งแรกที่กดเมนู Google จะขอสิทธิ์อีกรอบ — กด Allow)

**ในเบราว์เซอร์** — เปิด URL `/exec` ตัวเดิมตรง ๆ จะได้ตัวเลขสรุปเป็น JSON

---

## เก็บอะไรบ้าง / ไม่เก็บอะไร

| เก็บ | ไม่เก็บ |
|---|---|
| เวลาที่เกิดเหตุการณ์ | ชื่อ อีเมล ตัวตนของผู้ใช้ |
| ชนิดเหตุการณ์ (`open` / `mysignal`) | IP address |
| รายละเอียด (`on` = เข้าโหมดได้, `locked` = เจอรหัส) | รหัสผ่านที่พิมพ์ |
| ไอดีสุ่ม 12 ตัวประจำแท็บ ไว้แยกว่าคนละคน | หุ้นที่ดู |

**หมายเหตุเรื่องการนับ** — นับเป็น "แท็บ" ไม่ใช่ "คน" คนเดิมเปิดใหม่พรุ่งนี้
หรือเปิดจาก iPad กับคอมก็นับเป็นคนละอัน ตัวเลขจึงเป็นค่าประมาณขาสูง
ถ้าเน็ตหลุดหรือชีตล่ม แอปจะทำงานปกติแค่ไม่ได้บันทึกรอบนั้น

## ปิดการนับ

ลบ (หรือคอมเมนต์) บรรทัด `analytics_url` ใน Secrets — แอปกลับไปไม่นับอะไรเลย
โดยไม่ต้องแก้โค้ด
